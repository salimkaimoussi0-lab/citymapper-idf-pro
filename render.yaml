services:
  - type: web
    name: citymapper-idf
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: IDFM_API_KEY
        sync: false
